#include <stdio.h>
/**
 *main- uses printf to print a string on stdout
 *Return: returns 0 if successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
